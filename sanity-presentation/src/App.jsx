import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'
import sanityClient from '../sanityio-presentation/sanity.cli'
import imageUrlBuilder from '@sanity/image-url'
/*
To use sanityClient
sanityClient.fetch(``)

const urlBuilder = imageurlBuilder(sanityClient)
const imageRef = urlbuilder.image(image).url
To use Image url Builder
use imageRef as the path for the src in image tag 
*/

function App() {

  //useEffect fetch

  return (
    // content out
    <></>
  )
}

export default App
